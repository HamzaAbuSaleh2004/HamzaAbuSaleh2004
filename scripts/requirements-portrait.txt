# Portrait pipeline deps -- LOCAL ONLY, never installed in CI.
# rembg pulls onnxruntime (~hundreds of MB); the daily job doesn't need it
# because the ASCII portrait is generated once and committed.
pillow
numpy
opencv-python
# The [cpu] extra is what actually pulls onnxruntime; bare `rembg` installs the
# library but leaves it with no inference backend on recent versions.
rembg[cpu]
